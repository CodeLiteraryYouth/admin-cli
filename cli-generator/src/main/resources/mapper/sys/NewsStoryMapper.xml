<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dmj.cli.mapper.sys.NewsStoryMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.dmj.cli.domain.NewsStory">
            <id column="id" property="id" />
            <result column="title" property="title" />
            <result column="story_desc" property="storyDesc" />
            <result column="cover_url" property="coverUrl" />
            <result column="view_num" property="viewNum" />
            <result column="creator" property="creator" />
            <result column="create_time" property="createTime" />
            <result column="updater" property="updater" />
            <result column="update_time" property="updateTime" />
            <result column="delete_flag" property="deleteFlag" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            id, title, story_desc, cover_url, view_num, creator, create_time, updater, update_time, delete_flag
        </sql>
    <select id="listNewsStory" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"></include>
        from news_story where delete_flag=false
        <if test="searchVal != null and searchVal !=''">
            and title like CONCAT(#{searchVal},'%')
        </if>
    </select>

</mapper>
