<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dmj.cli.mapper.sys.TOrderMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.dmj.cli.domain.vo.sys.TOrderVO">
            <id column="id" property="id" />
            <result column="user_id" property="userId" />
            <result column="nick_name" property="userName" />
            <result column="code" property="code" />
            <result column="amount" property="amount" />
            <result column="payment_type" property="paymentType" />
            <result column="status" property="status" />
            <result column="create_time" property="createTime" />
            <result column="trade_no" property="tradeNo" />
            <result column="sku_id" property="skuId" />
            <result column="sku_type" property="skuType" />
            <result column="num" property="num" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            o.id, o.user_id, o.code, o.amount, o.payment_type, o.status, o.create_time, o.trade_no,
            u.nick_name,od.sku_id,od.sku_type,od.num
        </sql>
    <select id="listOrder" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"></include>
        from t_order o
        left join t_order_detail od on o.id=od.order_id
        left join user_info u on o.user_id=u.id
        where 1=1
        <if test="userId != null">
            and o.user_id=#{userId}
        </if>
        <if test="paymentType != null">
            and o.payment_type = #{paymentType}
        </if>
        <if test="status != null">
            and o.status = #{status}
        </if>
    </select>

</mapper>
